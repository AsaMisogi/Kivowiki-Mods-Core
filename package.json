{
  "name": "kivowiki-mods-core",
  "version": "1.4.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "npm --prefix modules/quick-tools run build",
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.js"
  }
}
